import styled from 'styled-components';

const H1 = styled.h1`
  font-family: var(--font-sans);
  color: inherit;
  line-height: 1.5;
  font-size: 3rem;
  letter-spacing: -0.066875rem;
  font-weight: 700;
`;

export { H1 };
