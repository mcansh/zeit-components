import styled from 'styled-components';

const H3 = styled.h3`
  font-family: var(--font-sans);
  color: inherit;
  line-height: 1.5;
  font-size: 1.5rem;
  letter-spacing: -0.029375rem;
  font-weight: 600;
`;

export { H3 };
